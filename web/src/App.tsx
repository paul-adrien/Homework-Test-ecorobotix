export function App() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-4 p-6 text-center">
      <h1 className="text-3xl font-semibold text-[var(--color-dark-navy)]">AgriWatch</h1>
      <p className="text-[var(--color-text-secondary)]">
        Weather monitoring dashboard for field agents.
      </p>
      <p className="font-mono text-sm text-[var(--color-primary)]">
        Scaffolding ready — implement features per CLAUDE.md.
      </p>
    </main>
  );
}
